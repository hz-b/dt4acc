[pytest]
testpaths =
    tests/tango_tests
    tests/epics_tests

markers =
    integration: requires a running server
    slow: writes to devices and waits
    connectivity: basic reachability checks

log_cli = true
log_cli_level = WARNING
timeout = 30